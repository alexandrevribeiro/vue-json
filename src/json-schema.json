{
    "name": "string",   
    "required": "boolean",
    "minLength":"number",
    "maxLength":"number",
    "mask": "string",
    "metadata": {
        "type": "object",
        "object-props": {
            "required": "boolean",
            "minLength":"number",
            "maxLength":"number",
            "mask": "string"
        }
    }
}