
import JsonPropMetadata from './JsonPropMetadata'

export default class JsonObjectMetadata {

    /**
     * Array with the metadata of all JSON properties
     */
    propsMetadata: JsonPropMetadata[] = []
}